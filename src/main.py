# === main.py ===
# Entry point for the AWingDingUseCase app. 
# Cleanly launches the GUI, following Python best practices.

from src import gui  # Assumes you're running from root project dir with src/ as a module
